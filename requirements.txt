# Web framework and HTTP / extraction stack.
Django>=5.0
requests
brotli

# URL extraction.
newspaper3k
lxml_html_clean   # required at runtime by recent newspaper3k releases
trafilatura

# LLM stack (Phase 2): LangChain + LangGraph + Hugging Face inference endpoints.
langchain
langchain-core
langchain-huggingface
langgraph
huggingface_hub
